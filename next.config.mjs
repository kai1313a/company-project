/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    // reactStrictMode: true,
    // basePath: "/company-project",
    distDir: 'out',
}

export default nextConfig
